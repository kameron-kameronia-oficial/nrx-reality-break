(function(){const audios=Array.from(document.querySelectorAll('audio'));audios.forEach(audio=>audio.addEventListener('play',()=>audios.forEach(other=>{if(other!==audio)other.pause()})));document.querySelectorAll('[data-lightbox]').forEach(button=>button.addEventListener('click',()=>{const dialog=document.querySelector('[data-lightbox-dialog]'),image=document.querySelector('[data-lightbox-image]');if(dialog&&image){image.src=button.dataset.lightbox;dialog.showModal()}}));document.querySelector('[data-lightbox-close]')?.addEventListener('click',()=>document.querySelector('[data-lightbox-dialog]')?.close());document.querySelector('[data-lightbox-dialog]')?.addEventListener('click',event=>{if(event.target===event.currentTarget)event.currentTarget.close()})})();
